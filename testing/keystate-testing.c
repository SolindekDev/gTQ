#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

int main(int argc, char **argv) {
    // while(1) {
        short is = GetKeyState(VK_ESCAPE);
    //     printf("%hd", is);
    // }

    
}